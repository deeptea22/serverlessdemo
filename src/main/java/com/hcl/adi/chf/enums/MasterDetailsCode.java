package com.hcl.adi.chf.enums;

/**
 * Enumeration to define master code
 * MED(Medications), COM(Comorbidities), PRO(Procedures), ADM(Admissions), IMP(Implants)
 * @author Shivendra
 */
public enum MasterDetailsCode {
	MED,
	COM,
	PRO,
	ADM,
	IMP;
}